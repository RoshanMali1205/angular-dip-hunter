export const environment = {
  production: false,
  // Optional local override. Production loads SUPABASE_URL + SUPABASE_ANON_KEY
  // from Netlify via /.netlify/functions/public-config.
  supabaseUrl: '',
  supabaseAnonKey: '',
};
